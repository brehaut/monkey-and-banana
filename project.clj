(defproject monkey-and-banana "1.0.0-SNAPSHOT"
  :description "This project contains multiple variations of the same program as    
                it is slowly transformed from a simple functional solution to a 
                logical solution."
  :dependencies [[org.clojure/clojure "1.2.0"]
                 [org.clojure/clojure-contrib "1.2.0"]
                 [unifycle "0.5.0"]] ; using unifycle because i cant
                                     ; find core.unify in a repo
  :dev-dependencies [[marginalia "0.3.2"]])
